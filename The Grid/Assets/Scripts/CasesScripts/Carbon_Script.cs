using UnityEngine;
using System.Collections;

public class Carbon_Script : Case {

	// Use this for initialization
	protected override void Start () {
		base.Start();
		goalTimes.Add ("Fire", 150f);
	}
	
	// Update is called once per frame
	protected override void Update () {
		base.Update ();
	
	}
}
